import { LightchangeDirective } from './lightchange.directive';

describe('LightchangeDirective', () => {
  it('should create an instance', () => {
    const directive = new LightchangeDirective();
    expect(directive).toBeTruthy();
  });
});
